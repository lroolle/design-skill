# <surface> -- <one line: what it is and what it is for>

<!-- A recipe is the shape of one kind of artifact, independent of the
design system that dresses it. Keep it language-agnostic: no token
values, no font names, no hues. Fill every section; 100-160 lines. -->

## Job

The single job this surface must make happen. Who uses it and how
often (once / weekly / daily / all day) -- frequency flips most
calls. Brand surface, product surface, or mixed (name which regions
are which).

## Protected functions

What must not break on this kind of surface, regardless of restyle:
the task path, the information actually read, labels and navigation,
legal copy, the conversion path. A verdict checks against this list.

## Structure

An ASCII wireframe of the desktop regions, then the mobile collapse
order (primary content first in source order). State the hierarchy:
what the eye lands on first, second, third. Name where the one
signature moment lives.

```
+----------------------------------------------+
| region                                       |
+----------------------------------------------+
```

Mobile: <order>.

## Settings

The pattern dials this surface turns the material to: type ratio,
density, color strategy, motion budget (from references/motion.md),
container and measure, grid, navigation model. Dials, not values.

## Directions

Two to four of the seven systems that fit and why (mechanism). One
that is a trap here and why.

## States

Loading, empty, error, success, partial/stale as they appear on this
surface, plus first-run. Each designed, each one sentence.

## Copy

What the words must do here: the headline's job, button verbs, the
empty-state sentence shape, error shape. One good / bad pair.

## Verify

Surface-specific P0 and P1 items on top of references/methods.md's
rubric. The three widths (390 / 768 / 1440). What to count before and
after on a restyle so "better" is a claim, not an adjective.

## Failure modes

Five to eight named ways this surface goes wrong: the template look,
the current AI look for this surface, the costume trap, and the
surface's own classic mistakes.
